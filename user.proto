syntax = "proto3";

package user;

option go_package="./user";

message AddReq {
  int64 userId = 1;
  string name = 2;
}

message AddResp {
  bool ok = 1;
}

service User {
  rpc AddUser(AddReq) returns (AddResp);
}